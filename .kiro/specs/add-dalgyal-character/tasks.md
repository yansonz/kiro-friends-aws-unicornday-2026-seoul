# Implementation Plan: 달걀귀신 캐릭터 추가 및 유형 체계 재설계

## Overview

달걀귀신(dalgyal) 캐릭터를 추가하고, 4축 매핑 테이블을 재설계하여 10캐릭터 체계로 전환한다. 타입 → 데이터 → 로직 → UI → 빌드 → 테스트 순서로 점진적으로 구현한다.

## Tasks

- [x] 1. 타입 시스템 및 캐릭터 데이터 업데이트
  - [x] 1.1 CharacterSlug 타입에 'dalgyal' 추가
    - `src/lib/types.ts`의 CharacterSlug 유니온 타입에 `'dalgyal'` 멤버 추가
    - _Requirements: 5.1_
  - [x] 1.2 달걀귀신 캐릭터 프로필 추가 및 기존 캐릭터 4축 값 변경
    - `src/data/characters.ts`에 달걀귀신 프로필 추가 (slug: 'dalgyal', emoji: '🥚', axisValues: {A:1, B:-1, C:1, D:-1})
    - 처녀귀신 axisValues를 {A:-1, B:-1, C:1, D:1}로 변경
    - 장승 axisValues를 {A:-1, B:1, C:1, D:-1}로 변경
    - 저승사자 axisValues를 {A:1, B:-1, C:-1, D:-1}로 변경
    - 10개 캐릭터의 시너지/긴장 관계를 design.md의 Synergy_Tension_Map에 따라 업데이트
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6, 2.4, 4.1, 4.2_
  - [ ]* 1.3 캐릭터 데이터 무결성 property 테스트 작성
    - **Property 1: 캐릭터 데이터 무결성**
    - **Validates: Requirements 1.6, 4.1**

- [x] 2. 퀴즈 엔진 매핑 테이블 및 결정 로직 업데이트
  - [x] 2.1 determineCandidatePool 매핑 테이블 재설계
    - `src/lib/quiz-engine.ts`의 mappingTable을 10개 단독 조합으로 변경
    - 기존 6개 조합 → 10개 조합 (모두 단독 배치)
    - _Requirements: 2.1, 2.2, 2.3, 2.5, 3.1_
  - [x] 2.2 determineCharacter 함수 단순화
    - 모든 매핑이 단독이므로 기존 3명/2명 그룹 분기 로직 제거
    - candidatePool[0] 반환으로 단순화 (향후 확장성 위해 length > 1 폴백 유지)
    - _Requirements: 3.2, 3.3_
  - [x] 2.3 euclideanFallback 함수 검증
    - 10개 캐릭터 프로필 기반으로 유클리드 거리 계산이 정상 동작하는지 확인
    - characters 배열을 순회하므로 달걀귀신 추가 시 자동 반영되지만, 로직 검증 필요
    - _Requirements: 3.4_
  - [ ]* 2.4 매핑 테이블 property 테스트 작성
    - **Property 2: 매핑 테이블 후보군 크기 제한**
    - **Property 3: 매핑 테이블 전체 커버리지**
    - **Validates: Requirements 2.1, 2.2, 2.3**
  - [ ]* 2.5 후보군 결정 및 폴백 property 테스트 작성
    - **Property 4: 후보군 결정 정확성**
    - **Property 5: 유클리드 폴백 유효성**
    - **Validates: Requirements 3.1, 3.4**

- [x] 3. Checkpoint - 핵심 로직 검증
  - Ensure all tests pass, ask the user if questions arise.

- [ ] 4. 전체 퀴즈 결과 유효성 테스트
  - [ ]* 4.1 getResult 전체 파이프라인 property 테스트 작성
    - **Property 6: 전체 퀴즈 결과 유효성**
    - **Validates: Requirements 3.5**

- [x] 5. UI 업데이트
  - [x] 5.1 랜딩 페이지 10캐릭터 원형 배치 업데이트
    - `src/app/page.tsx`의 세계관 소개 문구를 "10명의 캐릭터"로 변경
    - characters 배열 기반이므로 원형 배치 로직은 자동 반영
    - _Requirements: 6.1_
  - [x] 5.2 달걀귀신 결과 페이지 정적 생성 확인
    - `src/app/result/[type]/page.tsx`의 generateStaticParams가 allSlugs 기반이므로 자동 반영 확인
    - `getCharacterBySlug('dalgyal')` 정상 반환 확인
    - _Requirements: 6.3, 6.4_

- [x] 6. OG 이미지 생성 스크립트 확인
  - [x] 6.1 generate-og.ts가 달걀귀신 OG 이미지를 생성하는지 확인
    - `scripts/generate-og.ts`는 characters 배열을 순회하므로 자동 반영
    - 달걀귀신 캐릭터 이미지 파일(`public/characters/dalgyal-gwishin-kiro.png`)의 파일명을 `dalgyal.png`로 맞추거나, 기존 파일명에 맞게 로직 조정
    - _Requirements: 7.1, 7.2, 7.3_

- [x] 7. 기존 테스트 업데이트
  - [x] 7.1 기존 단위 테스트를 10캐릭터 기준으로 업데이트
    - 9개 캐릭터 기준 하드코딩된 값을 10개로 변경
    - 변경된 캐릭터(처녀귀신, 장승, 저승사자)의 axisValues 테스트 업데이트
    - 매핑 테이블 테스트를 재설계된 10개 조합으로 업데이트
    - _Requirements: 8.1, 8.2, 8.3, 8.4_

- [x] 8. Final checkpoint - 전체 테스트 통과 확인
  - Ensure all tests pass, ask the user if questions arise.

## Notes

- Tasks marked with `*` are optional and can be skipped for faster MVP
- `src/data/questions.ts`는 변경하지 않음 (8문항 구조 유지)
- `src/components/CharacterSlider.tsx`, `src/components/CharacterCard.tsx`는 characters 배열 기반이므로 코드 변경 불필요
- OG 이미지 생성 스크립트도 characters 배열 순회이므로 코드 변경 불필요 (캐릭터 이미지 파일명만 확인)
- 캐릭터 이미지 파일: `public/characters/dalgyal-gwishin-kiro.png`가 이미 존재하므로, slug 기반 파일명(`dalgyal.png`)으로 복사 또는 리네임 필요
