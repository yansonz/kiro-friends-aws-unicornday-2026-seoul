# Design Document: 달걀귀신 캐릭터 추가 및 유형 체계 재설계

## Overview

기존 Kiro Friends 9캐릭터 체계에 달걀귀신(dalgyal)을 10번째 캐릭터로 추가하고, 전체 유형 분류 시스템을 재설계한다. 핵심 문제는 갓쓴키로, 처녀귀신, 저승사자 3명이 동일한 4축 값(-1,-1,-1,-1)을 공유하여 보조 태그에 과도하게 의존하는 구조적 결함이다.

핵심 변경사항:
- 달걀귀신 캐릭터 프로필 추가 (slug: `dalgyal`, 이모지: 🥚, 컨셉: 추상화 장인/미니멀리스트)
- 일부 캐릭터의 4축 값 변경으로 3명 중복 해소
- 매핑 테이블 확장 (6개 → 8개 조합)
- 시너지/긴장 관계 10캐릭터 체계로 재설계
- Quiz Engine의 `determineCandidatePool`, `determineCharacter` 로직 업데이트
- UI 컴포넌트 10캐릭터 대응 (랜딩 페이지, 슬라이더, generateStaticParams)
- OG 이미지 11개 생성 (기본 1 + 캐릭터 10)

기술 스택 변경 없음: Next.js Static Export, TypeScript, Tailwind CSS, Chart.js, Jest + fast-check

## Architecture

기존 아키텍처는 변경하지 않는다. 변경 범위는 데이터 레이어와 로직 레이어에 한정된다.

```mermaid
graph TB
    subgraph "변경 대상 파일"
        T[src/lib/types.ts<br/>CharacterSlug 타입]
        C[src/data/characters.ts<br/>10개 캐릭터 프로필]
        QE[src/lib/quiz-engine.ts<br/>매핑 테이블 + 결정 로직]
        Q[src/data/questions.ts<br/>변경 없음]
    end

    subgraph "UI 변경 대상"
        LP[src/app/page.tsx<br/>10개 원형 배치]
        RP[src/app/result/type/page.tsx<br/>generateStaticParams]
        CS[src/components/CharacterSlider.tsx<br/>10개 슬라이더]
    end

    subgraph "빌드 스크립트"
        OG[scripts/generate-og.ts<br/>11개 OG 이미지]
    end

    subgraph "테스트"
        TS[src/__tests__/<br/>10캐릭터 기준 업데이트]
    end

    T --> C
    T --> QE
    C --> LP
    C --> RP
    C --> CS
    C --> OG
    QE --> TS
    C --> TS
end
```

### 변경 영향 범위

| 파일 | 변경 유형 | 설명 |
|---|---|---|
| `src/lib/types.ts` | 타입 추가 | CharacterSlug에 'dalgyal' 추가 |
| `src/data/characters.ts` | 데이터 추가/수정 | 달걀귀신 프로필 추가, 일부 캐릭터 axisValues 변경, 시너지/긴장 관계 업데이트 |
| `src/lib/quiz-engine.ts` | 로직 수정 | 매핑 테이블 재설계, determineCharacter 로직 업데이트 |
| `src/data/questions.ts` | 변경 없음 | 8문항 구조 유지 |
| `src/app/page.tsx` | UI 수정 | 10개 캐릭터 원형 배치 |
| `src/app/result/[type]/page.tsx` | 자동 반영 | allSlugs 기반이므로 characters 데이터 추가 시 자동 반영 |
| `src/components/CharacterSlider.tsx` | 자동 반영 | characters 배열 기반이므로 자동 반영 |
| `scripts/generate-og.ts` | 자동 반영 | characters 배열 순회이므로 자동 반영 |
| `src/__tests__/` | 테스트 수정 | 10캐릭터 기준으로 업데이트 |

## Components and Interfaces

### 1. 타입 변경 (types.ts)

```typescript
// CharacterSlug에 'dalgyal' 추가
export type CharacterSlug =
  | 'gatssn'
  | 'chonggak'
  | 'cheonyeo'
  | 'dokkaebi'
  | 'gumiho'
  | 'haetae'
  | 'jangseung'
  | 'jeoseung'
  | 'mulgwisin'
  | 'dalgyal';  // 추가
```

기존 인터페이스(AxisScores, AuxiliaryTags, KiroFeature, CharacterProfile)는 변경 없음.

### 2. Quiz Engine 변경 (quiz-engine.ts)

```typescript
// determineCandidatePool: 매핑 테이블 재설계
// 기존 6개 → 8개 조합으로 확장
function determineCandidatePool(scores: AxisScores): CharacterSlug[];

// determineCharacter: 2명 후보군 결정 로직 일반화
// 기존: 3명 그룹(gatssn/cheonyeo/jeoseung)과 2명 그룹(haetae/jangseung) 하드코딩
// 변경: 모든 2명 그룹에 대해 보조 태그 비교 로직 일반화
function determineCharacter(
  candidatePool: CharacterSlug[],
  tags: AuxiliaryTags
): CharacterSlug;
```

### 3. 캐릭터 데이터 변경 (characters.ts)

```typescript
// 달걀귀신 프로필 추가
const dalgyalProfile: CharacterProfile = {
  slug: 'dalgyal',
  emoji: '🥚',
  name: '달걀귀신형',
  title: '추상화 장인',
  // ... 전체 프로필
};

// 일부 기존 캐릭터의 axisValues 변경
// 처녀귀신: (-1,-1,-1,-1) → (-1,-1,-1,+1) 변경 검토
// 저승사자: (-1,-1,-1,-1) → (+1,-1,-1,-1) 변경 검토
```

## Data Models

### 4축 재설계 분석

현재 16가지 가능한 4축 조합 중 6개만 사용 중이다. 10개 캐릭터를 배치하려면 최소 6개 조합이 필요하고(2명씩 페어링 시), 이상적으로는 8~10개 조합을 사용하여 분류 정밀도를 높인다.

#### 재설계 원칙

1. 캐릭터의 핵심 성격과 4축 값이 의미적으로 일치해야 한다
2. 동일 4축 조합에 최대 2명까지만 배치한다
3. 2명이 같은 조합에 있을 경우, 보조 태그로 명확히 구분 가능해야 한다
4. 기존 캐릭터의 4축 값 변경은 최소화한다

#### 캐릭터별 4축 값 재설계

| 캐릭터 | slug | 기존 4축 | 변경 4축 | 변경 사유 |
|---|---|---|---|---|
| 갓쓴키로 | gatssn | (-1,-1,-1,-1) | (-1,-1,-1,-1) | 유지. 안정+구조+개인+품질 = 전략 설계자에 적합 |
| 총각귀신 | chonggak | (-1,+1,-1,+1) | (-1,+1,-1,+1) | 유지. 안정+즉흥+개인+속도 = 몰입 장인에 적합 |
| 처녀귀신 | cheonyeo | (-1,-1,-1,-1) | (-1,-1,+1,+1) | 변경. UX 수호자는 협업(사용자 피드백)과 속도(빠른 이터레이션) 성향이 강함 |
| 도깨비 | dokkaebi | (+1,+1,-1,+1) | (+1,+1,-1,+1) | 유지. 실험+즉흥+개인+속도 = 실험 개척자에 적합 |
| 구미호 | gumiho | (+1,-1,-1,+1) | (+1,-1,-1,+1) | 유지. 실험+구조+개인+속도 = 자동화 마법사에 적합 |
| 해태 | haetae | (-1,-1,+1,-1) | (-1,-1,+1,-1) | 유지. 안정+구조+협업+품질 = 품질 파수꾼에 적합 |
| 장승 | jangseung | (-1,-1,+1,-1) | (-1,+1,+1,-1) | 변경. 거버넌스 수문장은 프로세스 운영(즉흥적 대응)과 협업 성향이 강함 |
| 저승사자 | jeoseung | (-1,-1,-1,-1) | (+1,-1,-1,-1) | 변경. 부채 청산자는 기존 코드를 실험적으로 개선하는 성향 |
| 물귀신 | mulgwisin | (-1,+1,+1,+1) | (-1,+1,+1,+1) | 유지. 안정+즉흥+협업+속도 = 연결 촉진자에 적합 |
| 달걀귀신 | dalgyal | - | (+1,-1,+1,-1) | 신규. 실험+구조+협업+품질 = 추상화 장인에 적합 |

#### 변경 근거 상세

**처녀귀신 (-1,-1,-1,-1) → (-1,-1,+1,+1)**
- UX 수호자는 사용자와의 소통(협업)이 핵심이며, 빠른 프로토타이핑과 이터레이션(속도)을 중시
- 기존 "개인+품질"보다 "협업+속도"가 UX 디자이너/프론트엔드 개발자 성향에 더 부합

**장승 (-1,-1,+1,-1) → (-1,+1,+1,-1)**
- 거버넌스 수문장은 팀 프로세스를 유연하게 운영(즉흥)하면서 협업과 품질을 중시
- 해태와 같은 (-1,-1,+1,-1)에서 분리하여 독립 조합 확보
- "구조"보다 "즉흥"이 프로세스 관리자의 상황 대응 능력을 더 잘 표현

**저승사자 (-1,-1,-1,-1) → (+1,-1,-1,-1)**
- 부채 청산자는 기존 코드를 과감하게 리팩토링하는 실험적 성향이 있음
- 갓쓴키로와 분리하여 독립 조합 확보
- "안정"보다 "실험"이 레거시 코드 개선의 도전적 성격을 더 잘 표현

**달걀귀신 (+1,-1,+1,-1)**
- 추상화 장인은 새로운 패턴을 실험(실험)하되 체계적으로 구조화(구조)
- 팀의 코드 품질을 높이는 협업(협업)과 품질 중시(품질) 성향
- 미니멀리즘은 불필요한 것을 제거하는 구조적 사고와 품질 추구의 결합

#### 재설계된 4축 매핑 테이블

| 4축 조합 (A,B,C,D) | 부호 키 | Candidate Pool | 보조 태그 구분 |
|---|---|---|---|
| (안정,구조,개인,품질) | -1,-1,-1,-1 | [gatssn] | 단독 |
| (안정,즉흥,개인,속도) | -1,+1,-1,+1 | [chonggak] | 단독 |
| (실험,즉흥,개인,속도) | +1,+1,-1,+1 | [dokkaebi] | 단독 |
| (실험,구조,개인,속도) | +1,-1,-1,+1 | [gumiho] | 단독 |
| (안정,구조,협업,품질) | -1,-1,+1,-1 | [haetae] | 단독 |
| (안정,즉흥,협업,속도) | -1,+1,+1,+1 | [mulgwisin] | 단독 |
| (안정,즉흥,협업,품질) | -1,+1,+1,-1 | [jangseung] | 단독 |
| (실험,구조,개인,품질) | +1,-1,-1,-1 | [jeoseung] | 단독 |
| (안정,구조,협업,속도) | -1,-1,+1,+1 | [cheonyeo] | 단독 |
| (실험,구조,협업,품질) | +1,-1,+1,-1 | [dalgyal] | 단독 |

**개선 효과:**
- 기존: 6개 조합, 최대 3명 중복 → 변경: 10개 조합, 모두 단독 배치
- 보조 태그 의존도: 높음 → 없음 (4축만으로 캐릭터 결정 가능)
- 매핑 테이블 커버리지: 6/16 (37.5%) → 10/16 (62.5%)
- `determineCharacter` 함수의 복잡한 분기 로직이 불필요해짐

### 달걀귀신 캐릭터 프로필

```typescript
{
  slug: 'dalgyal',
  emoji: '🥚',
  name: '달걀귀신형',
  title: '추상화 장인',
  description: '복잡한 것을 단순하게, 본질만 남기는 미니멀리스트 개발자',
  axisValues: { A: 1, B: -1, C: 1, D: -1 },
  primaryTag: '추상화',
  strengths: [
    '복잡한 시스템에서 핵심 패턴을 추출하는 능력이 탁월하다',
    '깔끔한 인터페이스와 API 설계로 팀 전체의 생산성을 높인다',
    '불필요한 코드를 과감하게 제거하여 유지보수성을 극대화한다',
  ],
  pitfalls: [
    '과도한 추상화로 오히려 이해하기 어려운 코드를 만들 수 있다',
    '"이건 더 단순하게 만들 수 있어"가 끝없이 반복될 수 있다',
    '구체적인 구현보다 추상적 설계에 시간을 더 쓸 수 있다',
  ],
  kiroFeatures: [
    { name: 'Specs', description: '복잡한 요구사항을 핵심 인터페이스로 정제하여 설계' },
    { name: 'Steering Rules', description: '코드 복잡도 제한과 추상화 패턴 규칙을 AI에게 전달' },
    { name: 'Autopilot Mode', description: '반복적인 보일러플레이트 제거와 리팩토링을 자동화' },
  ],
  aiTips: [
    '"이 모듈의 공개 인터페이스를 최소화하는 방법을 제안해줘"',
    '"이 코드에서 불필요한 추상화 레이어를 찾아줘"',
    '"이 복잡한 로직을 더 단순한 패턴으로 리팩토링해줘"',
  ],
  synergy: 'gatssn',   // 전략 설계자 + 추상화 장인 = 완벽한 아키텍처
  tension: 'mulgwisin', // 연결 촉진자의 통합 지향 vs 미니멀리스트의 제거 지향
}
```

### 시너지/긴장 관계 재설계 (10캐릭터)

| 캐릭터 | 시너지 | 시너지 근거 | 긴장 | 긴장 근거 |
|---|---|---|---|---|
| 갓쓴키로 | 도깨비 | 전략 + 실험 = 혁신적 설계 | 총각귀신 | 설계 우선 vs 몰입 우선 |
| 총각귀신 | 구미호 | 몰입 + 자동화 = 극한 생산성 | 저승사자 | 새 코드 몰입 vs 기존 코드 청산 |
| 처녀귀신 | 해태 | UX 품질 + 테스트 품질 = 완성도 | 도깨비 | 완성도 추구 vs 빠른 실험 |
| 도깨비 | 갓쓴키로 | 실험 + 전략 = 혁신적 설계 | 해태 | 빠른 실험 vs 안정성 추구 |
| 구미호 | 총각귀신 | 자동화 + 몰입 = 극한 생산성 | 장승 | 자동화 자유 vs 프로세스 규칙 |
| 해태 | 저승사자 | 테스트 + 리팩토링 = 안정적 개선 | 도깨비 | 안정성 vs 실험 |
| 장승 | 달걀귀신 | 거버넌스 + 추상화 = 체계적 단순화 | 구미호 | 프로세스 규칙 vs 자동화 자유 |
| 저승사자 | 해태 | 리팩토링 + 테스트 = 안정적 개선 | 총각귀신 | 기존 코드 청산 vs 새 코드 몰입 |
| 물귀신 | 구미호 | 연결 + 자동화 = 통합 자동화 | 달걀귀신 | 연결 확장 vs 미니멀 제거 |
| 달걀귀신 | 갓쓴키로 | 추상화 + 전략 = 완벽한 아키텍처 | 물귀신 | 미니멀 제거 vs 연결 확장 |

### AuxiliaryTags 타입 변경

달걀귀신의 핵심 보조 태그는 '추상화'이다. 그러나 모든 캐릭터가 단독 4축 조합을 가지게 되므로, 보조 태그는 결과 페이지 표현용으로만 사용되고 캐릭터 결정 로직에서는 사용되지 않는다. 따라서 AuxiliaryTags 타입에 '추상화'를 추가할 필요는 없다. 기존 Q5~Q8 질문 구조도 변경하지 않는다.

### determineCharacter 로직 단순화

모든 캐릭터가 단독 4축 조합을 가지므로, `determineCharacter` 함수의 복잡한 분기 로직이 대폭 단순화된다:

```typescript
function determineCharacter(
  candidatePool: CharacterSlug[],
  tags: AuxiliaryTags
): CharacterSlug {
  // 모든 매핑이 단독이므로 첫 번째 캐릭터 반환
  return candidatePool[0];
}
```

기존의 3명 그룹/2명 그룹 분기 로직은 제거된다. 다만 향후 확장성을 위해 candidatePool.length > 1인 경우의 폴백 로직은 유지할 수 있다.



## Correctness Properties

*A property is a characteristic or behavior that should hold true across all valid executions of a system—essentially, a formal statement about what the system should do. Properties serve as the bridge between human-readable specifications and machine-verifiable correctness guarantees.*

### Property 1: 캐릭터 데이터 무결성

*For any* CharacterProfile in the characters 배열, 해당 프로필은 slug, emoji, name, title, description, axisValues, primaryTag, strengths(3개 이상), pitfalls(3개 이상), kiroFeatures(3개 이상), aiTips(3개 이상) 필드가 모두 존재하고 비어있지 않아야 하며, synergy와 tension 필드는 유효한 CharacterSlug(10개 중 하나)이어야 하고, 자기 자신을 참조하지 않아야 한다.

**Validates: Requirements 1.6, 4.1**

### Property 2: 매핑 테이블 후보군 크기 제한

*For any* 매핑 테이블 엔트리에 대해, 해당 Candidate_Pool의 크기는 1 이상 2 이하여야 한다.

**Validates: Requirements 2.1, 2.2**

### Property 3: 매핑 테이블 전체 커버리지

*For any* CharacterProfile의 axisValues에 대해, determineCandidatePool 함수를 호출하면 해당 캐릭터가 반환된 Candidate_Pool에 포함되어야 한다.

**Validates: Requirements 2.3**

### Property 4: 후보군 결정 정확성

*For any* 유효한 AxisScores(각 축 값이 임의의 정수)에 대해, determineCandidatePool 함수는 빈 배열(폴백 대상) 또는 매핑 테이블에 정의된 유효한 CharacterSlug 배열을 반환해야 한다.

**Validates: Requirements 3.1**

### Property 5: 유클리드 폴백 유효성

*For any* 매핑 테이블에 없는 4축 점수에 대해, euclideanFallback 함수는 10개 유효한 CharacterSlug 중 하나를 반환해야 한다.

**Validates: Requirements 3.4**

### Property 6: 전체 퀴즈 결과 유효성

*For any* 유효한 8문항 응답 조합(Q1~Q4는 0 또는 1, Q5~Q8은 0~3)에 대해, getResult 함수는 항상 10개 유효한 CharacterSlug 중 하나를 반환해야 한다.

**Validates: Requirements 3.5**

## Error Handling

### 기존 에러 처리 유지

기존 Kiro Friends의 에러 처리 전략은 그대로 유지한다:

- 유효하지 않은 슬러그(`/result/invalid`) 접근 시 404 페이지 (Next.js `notFound()`)
- 퀴즈 중간 이탈 시 처음부터 재시작
- 카카오 SDK 로드 실패 시 해당 버튼 비활성화

### 추가 에러 처리

- `getCharacterBySlug('dalgyal')`이 undefined를 반환하는 경우: characters 배열에 달걀귀신이 누락된 경우이므로 빌드 타임에 감지 (TypeScript 컴파일 에러)
- 매핑 테이블에 달걀귀신의 4축 조합이 누락된 경우: euclideanFallback으로 자동 처리되지만, 테스트에서 사전 감지

## Testing Strategy

### 단위 테스트 (Unit Tests)

테스트 프레임워크: Jest + React Testing Library

단위 테스트 대상:
- 달걀귀신 프로필 데이터 존재 및 필수 필드 확인
- `getCharacterBySlug('dalgyal')` 반환값 확인
- `allSlugs` 배열에 'dalgyal' 포함 및 길이 10 확인
- 재설계된 매핑 테이블의 각 조합별 정확한 후보군 확인
- 변경된 캐릭터(처녀귀신, 장승, 저승사자)의 axisValues 확인
- `determineCandidatePool`에 달걀귀신의 4축 값 입력 시 ['dalgyal'] 반환 확인
- `generateStaticParams`가 10개 슬러그를 반환하는지 확인

### 속성 기반 테스트 (Property-Based Tests)

테스트 라이브러리: fast-check (이미 프로젝트에 설치됨)

각 property 테스트는 최소 100회 반복 실행한다.

각 테스트에는 다음 형식의 태그 주석을 포함한다:
```
// Feature: add-dalgyal-character, Property N: [property title]
```

Property 테스트 목록:
1. **Property 1**: 10개 캐릭터 전체에 대해 필수 필드 존재, synergy/tension 유효성 검증
2. **Property 2**: 매핑 테이블의 모든 엔트리에 대해 후보군 크기 1~2 검증
3. **Property 3**: 10개 캐릭터의 axisValues로 determineCandidatePool 호출 시 자기 자신 포함 검증
4. **Property 4**: 임의의 AxisScores 생성 → determineCandidatePool 결과가 빈 배열 또는 유효한 CharacterSlug 배열
5. **Property 5**: 매핑 테이블에 없는 임의의 4축 점수 → euclideanFallback 결과가 유효한 CharacterSlug
6. **Property 6**: 임의의 유효한 8문항 응답 → getResult 결과가 10개 CharacterSlug 중 하나
